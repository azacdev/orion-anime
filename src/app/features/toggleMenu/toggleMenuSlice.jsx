import { createSlice } from "@reduxjs/toolkit";

const initialState = {
  toggleMenu: false 
}

export const toggleMenuSlice = createSlice({
  name: "toggleMenu",
  initialState,
  reducers: {

  }
})